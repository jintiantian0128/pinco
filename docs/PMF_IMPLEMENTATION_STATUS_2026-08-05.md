# Pinco PMF 路线图实施状态

更新时间：2026-08-13

判定口径：`代码完成`、`自动化通过`、`本地真实 HTTP 通过`、`线上已部署`、`微信页面通过`、`真机通过`是不同状态。后四项没有证据时不得合并写成“已完成”。

## 已完成代码与自动化回归

- P0 可信基础：稳定微信/设备身份；最多 5 个并行设备会话令牌；用户接口鉴权；微信云托管 MySQL 持久化适配；模型失败透明；岗位只接受带 HTTP(S) 原始链接的公开来源；ASR WebM/Opus 转码；隐私声明；数据导出、云端清空和永久删除；专家有未结算服务时禁止注销，已完成订单仅保留去标识化履约/财务记录。
- 求职闭环：首次四步作战台引导；目标画像与求职期限；真实证据库；AI 产品/应用工程/运营解决方案三类方向的八维证据雷达；岗位对象与云端进度；GO/MAYBE/NO_GO 投递判断；证据绑定、可编辑和可复制材料；可直接用/小改/重做/疑似编造反馈；简历诊断历史；按真实最低证据维度生成并持久化的 7 天补缺计划。
- 面试前练习：任务公司/轮次/日期/焦虑点建档；可选陪我热身、真实强度、压力追问；卡住时可取一次不代答、不推进题目的三步救场框架；5 分钟快速诊断、10 分钟同一弱项复练、20 分钟项目深挖、30 分钟全真模拟；自适应追问；内容、结构、证据、岗位匹配、表达清晰、临场应变六维评分；相邻回答和首尾回答量化对比；完成后可匿名发布不含原始回答的结构化复盘。
- 情感支持：跨首页、会话、投递等待、一面/二面/HR 面、Offer、拒绝和练习场景；状态变化后的陪伴可跳过，不强迫聊天；四种陪伴偏好；正文记忆单独授权；1–5 状态打卡；低落 24 小时回访；帮助度与 1–5 被理解程度反馈；回访时记录用户明确回答的微行动完成情况；危机提示与 12356/120/110 边界。
- 学社：云端发布、干货选项、评论、点赞/抱抱、直接召唤学姐、发布弹窗内的岗位可选绑定、面试轮次/发生时间经验标签、内容转结构化练习；官方示例明确标注且无虚构互动；三名不同用户举报后进入待审；管理员可发布、精选或隐藏；真实内容带动他人行动和人工精选形成幂等贡献记录，不承诺兑换。
- 专家市场：真实申请资料和证明链接、管理员审核、固定服务包、真实档期、预约与岗位可选绑定、接单、取消、交付摘要、下一步行动清单、真实评价；用户可单次授权向专家分享关联岗位、已确认证据和练习摘要，默认不分享；未审核专家不展示；内测不扣款，取消明确为无需退款。
- 商业化与 PMF：免费内测真实用量；已付费方案由服务端强制月额度，提前续费不能清零用量；未开放时付费方案标为假设并只登记意向；已实现默认关闭的微信支付下单、服务端签名、通知验签/解密、金额核对、幂等开通、查询/关单和未交付专家服务全额退款；测试账号、实付验收、正式售卖三层开关；“我的”页采集首批用户的专业推进价值、被理解感和未来 7 天继续使用意愿，每位用户只保留最近一次回答；管理员 PMF 汇总只给真实计数和服务端确认数据，没有样本时返回空值。

## 已取得的验证证据

- 后端完整测试：38 项通过；除既有可信基础覆盖外，包含 MySQL 建表/UTF-8 状态往返/健康检查、运营工作台安全头、令牌不落本地存储、举报正文受限上下文，以及共创反馈更新、指标去重、注销清理和错误意愿校验。
- 前端 `critical-flows.test.mjs`：通过；包含小程序作战台、岗位贯通、发布弹窗岗位绑定、专家摘要授权/交付清单、贡献状态、材料质量反馈、弱项前后对比，以及长 JD 不进入微信页面跳转 URL 的静态护栏。
- `npm run build:weapp`：通过；只有 Sass `@import` 弃用警告，不阻塞构建。
- 本地真实 HTTP：`0.6.0` 健康检查、启动身份、缺令牌 401、岗位→学社→另一用户启动行动→作者贡献 +3、数据导出、永久删除均通过，测试账号自动清理。该主链证据取得于新增面试强度/救场端点之前；新增代码随后已通过 30 项自动化、语法、关键页面检查和微信构建，但最终版 HTTP 重跑被 Codex 权限审批额度限制拦截，不能把新增端点记为 HTTP 已通过。由于本机 DeepSeek Key 已失效，模型链明确记为跳过，不能计为模型通过。
- 最新安全部署包：`backend/pinco-backend-upload-20260813-155439-safe.zip`；只含 5 个运行文件（含运营工作台），压缩包完整性和敏感模式扫描通过；SHA-256 `4ced5bc6d232939b4664a9ee353c9d77652bf17a26ddff5c9a5cff3176b96892`。
- 2026-08-13 共创反馈真实 HTTP 回归：缺少用户会话令牌时返回 401；有效会话提交与回读一致；管理员指标按 1 位用户最近一次反馈计数；测试账号及关联反馈随后删除。微信自动化端口可连接和切页，但 Mac 锁屏后页面节点读取超时，因此不能记为微信页面验收通过。
- 当前公网健康检查返回 `0.6.0`，DeepSeek `deepseek-chat` 为 `connected`。2026-08-10 使用自动清理测试账号实际触发并通过会话 DeepSeek、结构化面试、401 会话边界、岗位状态情感支持、岗位→学社→行动贡献、导出脱敏和账号删除。
- 2026-08-11 微信开发者工具已完成会话与剪贴板页面验证：普通对话连续返回真实模型内容；新增的会话失效自动恢复使用仅限 develop 环境的测试钩子连续三轮得到 `/api/v1/chat` 401，并在不刷新页面、不清缓存的前提下自动 bootstrap、改写旧 user_id、只重试一次并得到模型回复；同一模型回复连续复制三次，均显示“已复制”且回读内容完全一致。该证据只覆盖开发者工具，不替代 iOS/Android 真机。
- 2026-08-11 学社页面验证：发布弹窗真实显示“干货”类型；当前 bootstrap 身份不经过额外登录即可点击“召唤学姐”，服务端 `/summon` 返回 200，帖子下出现 Pinco 学姐模型回复。召唤会写入社区数据，为避免测试污染只执行一次，不计为三次验证。
- 2026-08-11 附件页面验证未通过：开发者工具点击“发送图片”后，微信在进入系统选图前返回 errno 112，明确提示 `chooseImage` 未在当前 AppID 的隐私保护指引中声明。该项是微信平台配置阻塞，不是后端上传成功；补齐“选中的照片或视频信息”后必须重跑。PDF/DOCX 所需“选中的文件”以及录音所需“麦克风”也需一并核对，尚未记为通过。
- 2026-08-11 公网 readiness 为 4/7：微信 AppID、AppSecret、合法请求域名和 DeepSeek 已生效；云端持久化、语音三端实录、管理员治理令牌仍未就绪。

## 仍需外部条件或人工界面验收

1. 云托管已切到 0.6.0；仍需从微信页面验证完整对话、材料生成和面试练习。
2. 部署 MySQL 版本并配置 `PINCO_STATE_BACKEND=mysql`、至少 32 位管理员令牌。微信 AppID/AppSecret/合法域名已在 readiness 中确认生效；部署和重启回读验证前，线上状态仍是非持久容器文件。
3. 开发者工具的对话自动恢复与复制已连续三次通过，学社发布类型与召唤已通过一次真实页面链路；录音、PDF/DOCX、岗位、面试、专家申请仍需连续三次，不得只测后端。
4. 先在当前 AppID 的隐私保护指引中补齐并保存“选中的照片或视频信息”“选中的文件”“麦克风”，确认审核/同步生效；随后在开发者工具、iOS 与 Android 各连续三次验证录音、剪贴板、图片和文件选择。通过前保持 `ASR_DEVICE_VERIFIED=false`。
5. 微信支付代码已完成本地自动化，但仍需要商户号、APIv3 密钥、商户私钥、平台公钥和回调地址；必须由白名单账号完成小额实付、异步回调、主动查询、关单和全额退款真机验收。验收前所有售卖开关继续关闭，不用假支付替代。
6. 图片视觉理解需要选择并配置视觉/OCR 服务；目前只做真实上传校验和本地预览，明确模型不能读图。
7. PMF 只能由首批真实用户数据验证。产品内共创反馈与运营指标已经具备，但不能替代 10 人设计伙伴试点、访谈和付费验证。

## 当前阻塞

- 公网已是 MySQL 持久化版本且至少 32 位管理员令牌已配置；包含运营工作台与共创反馈接口的最新包尚未部署，iOS/Android 真机实录仍未就绪。
- iOS/Android 真机的录音、剪贴板和文件选择需要用户扫码配合；在真机证据取得前不得写为通过。
