# Pinco PMF 路线图实施状态

更新时间：2026-08-13

判定口径：`代码完成`、`自动化通过`、`本地真实 HTTP 通过`、`线上已部署`、`微信页面通过`、`真机通过`是不同状态。后四项没有证据时不得合并写成“已完成”。

## 已完成代码与自动化回归

- P0 可信基础：稳定微信/设备身份；最多 5 个并行设备会话令牌；用户接口鉴权；微信云托管 MySQL 持久化适配；模型失败透明；岗位只接受带 HTTP(S) 原始链接的公开来源；ASR WebM/Opus 转码；隐私声明；数据导出、云端清空和永久删除；专家有未结算服务时禁止注销，已完成订单仅保留去标识化履约/财务记录。
- 求职闭环：首次四步作战台引导；目标画像与求职期限；真实证据库；AI 产品/应用工程/运营解决方案三类方向的八维证据雷达；岗位对象与云端进度；GO/MAYBE/NO_GO 投递判断；证据绑定、可编辑和可复制材料；可直接用/小改/重做/疑似编造反馈；简历诊断历史；按真实最低证据维度生成并持久化的 7 天补缺计划。
- 面试前练习：任务公司/轮次/日期/焦虑点建档；可选陪我热身、真实强度、压力追问；卡住时可取一次不代答、不推进题目的三步救场框架；5 分钟快速诊断、10 分钟同一弱项复练、20 分钟项目深挖、30 分钟全真模拟；自适应追问；内容、结构、证据、岗位匹配、表达清晰、临场应变六维评分；相邻回答和首尾回答量化对比；完成后可匿名发布不含原始回答的结构化复盘。
- 情感支持：跨首页、会话、投递等待、一面/二面/HR 面、Offer、拒绝和练习场景；状态变化后的陪伴可跳过，不强迫聊天；四种陪伴偏好；正文记忆单独授权；1–5 状态打卡；低落 24 小时回访；帮助度与 1–5 被理解程度反馈；回访时记录用户明确回答的微行动完成情况；危机提示与 12356/120/110 边界。
- 学社：云端发布、干货选项、评论、点赞/抱抱、直接召唤学姐、发布弹窗内的岗位可选绑定、面试轮次/发生时间经验标签、内容转结构化练习；官方示例明确标注且无虚构互动；三名不同用户举报后进入待审；管理员可发布、精选或隐藏；真实内容带动他人行动和人工精选形成幂等贡献记录，不承诺兑换。
- 专家市场：真实申请资料和证明链接、管理员审核、固定服务包、真实档期、预约与岗位可选绑定、接单、取消、交付摘要、下一步行动清单、真实评价；用户可单次授权向专家分享关联岗位、已确认证据和练习摘要，默认不分享；未审核专家不展示；内测不扣款，取消明确为无需退款。
- 商业化与 PMF：免费内测真实用量；已付费方案由服务端强制月额度，提前续费不能清零用量；未开放时付费方案标为假设并只登记意向；已实现默认关闭的微信支付下单、服务端签名、通知验签/解密、金额核对、幂等开通、查询/关单和未交付专家服务全额退款；测试账号、实付验收、正式售卖三层开关；“我的”页采集首批用户的专业推进价值、被理解感和未来 7 天继续使用意愿，每位用户只保留最近一次回答；管理员 PMF 汇总只给真实计数和服务端确认数据，没有样本时返回空值。

## 已取得的验证证据

- 后端完整测试：46 项连续三轮通过；除既有可信基础外，覆盖 MySQL 建表/UTF-8 状态往返/健康检查、运营工作台安全头、令牌不落本地存储、举报正文受限上下文、共创反馈、职业记忆白名单与中英文 key 归一化、会话上下文、显式岗位检索意图、JobPosting 风格岗位来源门禁、关键求职节点去重、发布指纹和岗位词表。
- 前端 `critical-flows.test.mjs`：通过；包含小程序作战台、岗位贯通、发布弹窗岗位绑定、专家摘要授权/交付清单、贡献状态、材料质量反馈、弱项前后对比，以及长 JD 不进入微信页面跳转 URL 的静态护栏。
- `npm run build:weapp`：通过；只有 Sass `@import` 弃用警告，不阻塞构建。
- 本地真实 HTTP：v0.7.0 已完成身份 401/200、写入、社区、导出和永久删除闭环；职业记忆与服务端上下文由自动化覆盖。本机旧 DeepSeek Key 已失效，因此 v0.7.0 的真实模型链必须部署后使用云端密钥验证，不能用本地非模型链替代。
- 最新安全部署包：`backend/pinco-backend-upload-20260813-212736-safe.zip`；只含 6 个运行文件并包含 `career_taxonomy.py`，压缩包完整性和敏感模式扫描通过；SHA-256 `9b125835e84a2dc9ea51604f931b763b85db7e76d9205906def90a11199faa4d`。
- 2026-08-13 共创反馈真实 HTTP 回归：缺少用户会话令牌时返回 401；有效会话提交与回读一致；管理员指标按 1 位用户最近一次反馈计数；测试账号及关联反馈随后删除。微信自动化端口可连接和切页，但 Mac 锁屏后页面节点读取超时，因此不能记为微信页面验收通过。
- 当前公网健康检查返回 `0.7.0`，DeepSeek `deepseek-chat` 为 `connected`，MySQL `durable=true/online=true`。2026-08-13 使用自动清理测试账号连续三轮通过会话边界 401/200、社区写入、付费失败关闭、导出脱敏和账号删除。
- 2026-08-11 微信开发者工具已完成会话与剪贴板页面验证：普通对话连续返回真实模型内容；新增的会话失效自动恢复使用仅限 develop 环境的测试钩子连续三轮得到 `/api/v1/chat` 401，并在不刷新页面、不清缓存的前提下自动 bootstrap、改写旧 user_id、只重试一次并得到模型回复；同一模型回复连续复制三次，均显示“已复制”且回读内容完全一致。该证据只覆盖开发者工具，不替代 iOS/Android 真机。
- 2026-08-11 学社页面验证：发布弹窗真实显示“干货”类型；当前 bootstrap 身份不经过额外登录即可点击“召唤学姐”，服务端 `/summon` 返回 200，帖子下出现 Pinco 学姐模型回复。召唤会写入社区数据，为避免测试污染只执行一次，不计为三次验证。
- 隐私保护指引补齐后，微信开发者工具已完成图片上传 3 次、DOCX 上传 3 次、PDF 上传 1 次；录音→阿里云 ASR→文字回填完成 3 次。该证据只覆盖开发者工具，不替代 iOS/Android 真机。
- v0.7.0 已由 Actions 运行 `31712741669` 提交到云托管并异步上线；该运行的干净环境测试、微信构建、Docker 构建、CLI 登录、环境发现、基线、回滚目标和 dry-run 都通过。官方 CLI 在版本创建后因构建日志主题不存在而错误退出，因此该次 GitHub 运行仍显示失败；公网健康事实证明版本创建和异步部署成功，但“GitHub 工作流绿色通过”仍待修订版复跑。
- 线上真实 Agent 测试发现一个未通过项：用户明确说“请记住工作年限/目标岗位/城市”时，旧实现误触发岗位检索，模型口头确认但 `memory_updated=false`。PR 分支已改为只在明确搜索意图下调用岗位提供方、强制英文记忆 key、归一化中文 key，并在主 Agent 漏写时调用真实模型做受限结构化记忆提取；尚待新版部署后连续三轮公网复测，不能提前宣称已修复。
- 同一真实 Agent 失败响应还暴露岗位可信度问题：新闻和“什么是产品经理”科普页曾因 URL 可打开而被标成“已校验来源”。PR 分支已按 Google JobPosting 内容政策适配保守门禁：招聘主体、职位信号和 HTTP(S) 来源缺一不可，新闻/科普/榜单直接过滤；产品文案统一改为“带来源候选，请打开确认在招”，不再把链接存在写成真实在招。该修订尚未部署，不能计入线上通过。

## 仍需外部条件或人工界面验收

1. `WXCLOUD_APP_ID` 和 `WXCLOUD_PRIVATE_KEY` 已配置。先让修订后的手动工作流以精确 commit SHA 通过一次，再开启 `main` push 自动触发。
2. 部署职业记忆修订版后，先从公网连续三轮验证显式记忆、跨新问答复用和重进历史恢复，再从微信开发者工具验证跨会话上下文、简历记忆、模型判断关键节点、材料生成、完整面试和真实专家申请审核；不得用后端接口替代页面路径。
3. iOS 与 Android 各连续三次验证录音、剪贴板、图片和文件选择；通过前保持 `ASR_DEVICE_VERIFIED=false`。
4. 微信支付代码已完成本地自动化，但仍需要商户号、APIv3 密钥、商户私钥、平台公钥和回调地址；必须由白名单账号完成小额实付、异步回调、主动查询、关单和全额退款真机验收。验收前所有售卖开关继续关闭，不用假支付替代。
5. 图片视觉理解需要选择并配置视觉/OCR 服务；目前只做真实上传校验和本地预览，明确模型不能读图。
6. PMF 只能由首批真实用户数据验证。产品内共创反馈与运营指标已经具备，但不能替代 10 人设计伙伴试点、访谈和付费验证。

## 当前阻塞

- 本机 GitHub CLI 的旧令牌已失效；Git 推送仍正常，但重新触发/读取 Actions 需要完成一次 GitHub 网页设备授权。
- 公网已为 v0.7.0，但职业记忆修订版和精确 SHA 健康门禁尚未部署；真实 Agent 失败项必须在新工作流绿色通过后复测，不能用当前 v0.7.0 版本号代替提交级证据。
- iOS/Android 真机的录音、剪贴板和文件选择需要用户扫码配合；在真机证据取得前不得写为通过。
